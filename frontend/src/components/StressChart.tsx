import React from 'react'; import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'; import {ChartCard,EmptyState} from './ui';
export default function StressChart({rows}:{rows:any[]}){const data=(rows||[]).slice(0,8).map((r:any)=>({name:r.stress_case||r.scenario,impact:Number(r.portfolio_impact_pct||0)})); if(!data.length)return <ChartCard title='Стресс-тест'><EmptyState title='Нет данных stress-теста' description='Данные появятся после расчёта.'/></ChartCard>; return <ChartCard title='Стресс-тест' subtitle='Влияние сценариев'><div style={{height:220}}><ResponsiveContainer width='100%' height='100%'><BarChart data={data}><XAxis dataKey='name' hide/><YAxis/><Tooltip contentStyle={{background:'#0b1526',border:'1px solid #334155',color:'#e5edf7'}}/><Bar dataKey='impact' fill='#f59e0b' /></BarChart></ResponsiveContainer></div></ChartCard>}
