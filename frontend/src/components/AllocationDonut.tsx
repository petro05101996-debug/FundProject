import React from 'react'; import { PieChart, Pie, Cell, Tooltip, ResponsiveContainer } from 'recharts'; import {ChartCard,EmptyState} from './ui';
const COLORS=['#22d3ee','#f59e0b','#ef4444','#64748b','#22c55e'];
export default function AllocationDonut({items}:{items:any[]}){const data=(items||[]).map((i:any)=>({name:i.asset_class,value:Number(i.weight_pct||0)})); if(!data.length)return <ChartCard title='Структура активов'><EmptyState title='Нет данных allocation' description='Появится после расчёта.'/></ChartCard>; return <ChartCard title='Структура активов'><div style={{height:220}}><ResponsiveContainer width='100%' height='100%'><PieChart><Pie data={data} dataKey='value' nameKey='name' innerRadius={45} outerRadius={80}>{data.map((_:any,i:number)=><Cell key={i} fill={COLORS[i%COLORS.length]} />)}</Pie><Tooltip contentStyle={{background:'#0b1526',border:'1px solid #334155',color:'#e5edf7'}}/></PieChart></ResponsiveContainer></div></ChartCard>}
