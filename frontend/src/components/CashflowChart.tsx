import React from 'react'; import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'; import {ChartCard,EmptyState} from './ui';
export default function CashflowChart({rows}:{rows:any[]}){const data=(rows||[]).slice(0,12).map((r:any)=>({year:r.year,value:Number(r.value_before_stress||0)})); if(!data.length)return <ChartCard title='Денежные потоки'><EmptyState title='Нет данных cashflow' description='Данные появятся после расчёта.'/></ChartCard>; return <ChartCard title='Денежные потоки'><div style={{height:220}}><ResponsiveContainer width='100%' height='100%'><LineChart data={data}><XAxis dataKey='year'/><YAxis/><Tooltip contentStyle={{background:'#0b1526',border:'1px solid #334155',color:'#e5edf7'}}/><Line type='monotone' dataKey='value' stroke='#22d3ee' /></LineChart></ResponsiveContainer></div></ChartCard>}
